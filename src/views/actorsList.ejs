<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Celuloide Films</title>
		<link rel="stylesheet" href="/stylesheets/style.css">
	</head>

	<body>
		<%-include('partials/header')%>
	 		<div class="main"  >
				<h1>Listado de Películas de </h1>
					<h2><%= actor.first_name + " " + actor.last_name%></h2>
					<ul class="movieList">
                        <% actor.peliculas.forEach(movie => { %>
                            <li class="peli" > 
                                <a href="/movies/detail/<%= movie.id %>"> <%= movie.title %>
                                </a>
                            </li>
                        <% }) %>
					</ul>
			   </div>
	</body>
</html>